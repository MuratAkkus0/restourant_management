import { IconType } from 'react-icons';

export interface AdvantagesCardProps {
  title: string;
  desc?: string;
  className?: string;
  Icon?: IconType;
  iconSize?: number;
  iconColor?: string;
}
